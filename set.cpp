#include<bits/stdc++.h>
using namespace std ;
#define ll long long
int main()
{
    int t,i,j,k;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        vector<int> arr;
        set<int>s;
        for(i=0;i<n;i++)
        {
            cin>>k;
            arr.insert(k);
            s.insert(k);
        }
    }
}